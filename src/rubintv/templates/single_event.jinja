{% extends "cameras/layout.jinja" %}

{% block content %}
<div id="refresher">
  <div class="subheader">
    <h2>{{ date }} : Sequence {{ seq }}</h2>
  </div>
  {% if the_url | length %}
    <a href="{{ the_url }}">
      <img class="resp {% if channel=="Monitor" %} behind {% endif %}" src="{{ the_url }}">
    </a>
    {% if channel != "Monitor" %}
      <div class="desc">{{ name }}</div>
    {% endif %}
  {% else %}
    <p>Event not found</p>
  {% endif %}
</div>
{% endblock content %}
