{% extends "cameras/layout.jinja" %}
{% block content %}
<h2>Historical Data</h2>
<div class="columns">
  <section class="calendar">
      {% for year in years %}
        {% if year == year_to_display %}
        <div class="year open" data-year="{{ year }}">
        {% else %}
        <div class="year closed" data-year="{{ year }}">
        {% endif %}
          <h4 class="year-title">{{ year }}</h4>
            <div class="months">
              {% for month in years[year] %}
              <div class="month">
                <h5 class="month-title">
                  {{ month_names[month - 1] }}
                </h5>
                <div class="days">
                  {% for day in years[year][month] %}
                    <div class="day" data-date="{{ year }}-{{ month }}-{{ day }}">{{ day }}</div>
                  {% endfor %}
                </div>  {# end of days #}
              </div> {# end of month #}
            {% endfor %}
            </div>

        </div> {# end of year #}
      {% endfor %}
  </section>
  <section class="channel-day-data">
    {% include "cameras/day-data-per-day-channels.jinja" %}
  </section>
</div>
{% endblock content %}
{% block footer_scripts %}
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
<script type=text/javascript src="/rubintv/static/js/calendar.js"></script>
{% endblock footer_scripts %}
